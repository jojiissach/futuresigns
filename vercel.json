{
    "builds": [{
        "src": "webproject/wsgi.py",
        "use": "@ardnt/python",
        "config": { "maxLambdaSize": "15mb", "runtime":"python3.12.3" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "webproject/wsgi.py"
        }
    ]
}