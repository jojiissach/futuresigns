{
    "builds": [{
        "src": "webproject/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "webproject/wsgi.py"
        }
    ]
}